import { generateMetadata } from './metadata'

export { generateMetadata }

export default function ComplementaryMedicineEntryLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 