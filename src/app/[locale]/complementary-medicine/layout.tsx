import { generateMetadata } from './metadata'

export { generateMetadata }

export default function ComplementaryMedicineLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 